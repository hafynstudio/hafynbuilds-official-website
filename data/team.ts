import type { TeamMember } from "@/types/team-member";

// Populated in Phase 7. Real names/roles/photos/specialties for the 4
// team members are pending from the user per PRD §5 — this file's shape
// (TeamMember[]) will not change when that content arrives.
export const team: TeamMember[] = [];
