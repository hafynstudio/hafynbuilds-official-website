import type { CurrencyRate } from "@/types/currency";

// Populated in Phase 10 with the ~20-30 supported country/currency
// mappings (PKR, INR, EUR, GBP, AED, etc.), each rate relative to USD.
export const currencyRates: CurrencyRate[] = [];
