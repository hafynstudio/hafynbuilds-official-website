import { buildMetadata } from "@/lib/seo/metadata";

export const metadata = buildMetadata({
  title: "Capabilities",
  path: "/capabilities",
});

export default function CapabilitiesPage() {
  return (
    <main>
      <h1>Capabilities — built in Phase 8</h1>
    </main>
  );
}
