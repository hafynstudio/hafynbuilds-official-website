import { buildMetadata } from "@/lib/seo/metadata";

export const metadata = buildMetadata({
  title: "Method",
  path: "/method",
});

export default function MethodPage() {
  return (
    <main>
      <h1>Method — built in Phase 9</h1>
    </main>
  );
}
